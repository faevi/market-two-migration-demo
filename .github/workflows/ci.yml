name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  migration-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1

      # 2) Node.js 23.11.0
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 23.11.0

      - name: Enable Corepack and set Yarn version
        run: |
          corepack enable
          corepack prepare yarn@4.9.1 --activate

      # 3) Solana CLI 2.2.12
      - name: Install Solana CLI
        run: |
          curl -sSfL https://release.solana.com/v2.2.12/install | sh
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
      # 4) Install Anchor CLI (latest)
      - name: Install Anchor CLI
        run: |
          cargo install --git https://github.com/project-serum/anchor --tag v0.31.1 anchor-cli --locked
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          echo "::set-output name=installed::true"
          export PATH="/home/runner/.local/share/solana/install/active_release/bin:$PATH"

      - name: Install dependencies
        run: yarn install

      - name: Build program (simple)
        run: yarn build-simple-test

      - name: Run simple tests
        run: yarn test --skip-lint

      - name: Build program (migration)
        run: yarn build-migration

      - name: Run migration tests
        run: yarn test --skip-lint
